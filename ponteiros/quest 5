#include <stdio.h>

int maior(int *a, int *b) {
    if (*a > *b)
        return *a;
    else
        return *b;
}

int main() {
    int x = 10, y = 20;

    printf("Maior: %d\n", maior(&x, &y));

    return 0;
}
