#include <stdio.h>

int somaVetor(int *v, int n) {
    int soma = 0;

    for (int i = 0; i < n; i++) {
        soma += *(v + i);
    }

    return soma;
}

int main() {
    int v[5] = {1,2,3,4,5};

    printf("Soma: %d\n", somaVetor(v, 5));

    return 0;
}
